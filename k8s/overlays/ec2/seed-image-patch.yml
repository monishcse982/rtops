apiVersion: batch/v1
kind: Job
metadata:
  name: seed
  namespace: rtops
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: seed
          image: ghcr.io/monishcse982/rtops:main
          imagePullPolicy: Always
