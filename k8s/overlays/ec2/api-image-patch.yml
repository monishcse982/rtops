apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: rtops
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: api
          image: ghcr.io/monishcse982/rtops:main
          imagePullPolicy: Always
